Find scanner API/app that can fill in values in foodstores

- Add reminders for inventory items that are going to expire soon. Need to determine rules for "soon"
	- Also need alert for items running low on. This needs to be baked into the logic for the shopping list too, if its's not already
- Consolidate command palette actions into special master modal menu. A single command that opens a menu with all the commands listed so they don't clutter the palette. 
	- Determine which actions are best served being on the ribbon/mobile quick actions menu